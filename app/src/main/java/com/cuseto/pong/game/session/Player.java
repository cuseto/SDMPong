package com.cuseto.pong.game.session;

/**
 * Identifies one of the two sides in a match.
 */
public enum Player {
    /** The player controlling the paddle on the left side of the arena. */
    LEFT,
    /** The player controlling the paddle on the right side of the arena. */
    RIGHT
}
